type GameSettings = {
    genres: string[],
    playlists: string[],
    randomStartTime: boolean,
}
const gameSettings: GameSettings = {
    genres: [],
    playlists: [],
    randomStartTime: false,
}